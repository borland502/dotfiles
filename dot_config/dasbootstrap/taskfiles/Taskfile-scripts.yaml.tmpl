# chezmoi:template:left-delimiter="##" right-delimiter=##
version: "3"

vars:
  ##range .taskfile -##
  ##.##
  ##end##
  ##range .xdg -##
  ##.##
  ##end##

tasks:
  system:update:
    desc: Update the local system
    cmds:
      - zsh -c sysupdate.zsh


  lint:
    desc: Use linters on ansible related code.  Allows formatting when available
    cmds:
      - ansible-lint --profile=production --fix
      - shellcheck
      - shfmt
